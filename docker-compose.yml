services:
  app:
    build: ./app
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - db_data:/data/db
      - /Volumes/raydesktop/History/3_Life/2020-03-00_就活:/data/docs:ro    # host SMB mount point → container
    environment:
      - TZ=Asia/Tokyo

volumes:
  db_data:
